<Page
    x:Class="PruebaTecnica.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PruebaTecnica"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:PruebaTecnica.Converters"
    mc:Ignorable="d">
    <Page.Resources>
        <x:String x:Key="titulo">Prueba Técnica</x:String>
        <x:Double x:Key="tituloSize">100</x:Double>
        <x:String x:Key="guardarButton">Guardar</x:String>
        <converters:GenderConverter x:Key="GenderConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>

            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Text="{StaticResource titulo}" FontSize="{StaticResource tituloSize}"/>

            <ListView VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="1" x:Name="itemListView"
          ItemsSource="{Binding RootObject.Results}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="Auto" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="110" Height="110">
                                <Image>
                                    <Image.Source>
                                        <BitmapImage UriSource="{Binding Picture.medium}"/>
                                    </Image.Source>
                                </Image>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name.First}" TextWrapping="NoWrap" />
                                    <TextBlock Text="{Binding Name.Last}" TextWrapping="NoWrap" Margin="3,0,0,0" />
                                </StackPanel>
                                <TextBlock Text="{Binding Gender,Converter={StaticResource GenderConverter}}"  TextWrapping="NoWrap"/>
                                <TextBlock Text="{Binding Location.Street}"  TextWrapping="NoWrap"/>
                                <TextBlock Text="{Binding Email}"  MaxHeight="60"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Command="{Binding SaveCommand}" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Content="Guardar en SQLite"></Button>

            <ListView DataContext="{Binding RootDTOVm}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="3" x:Name="itemListViewSQLite"
          ItemsSource="{Binding RootDTOList}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="Auto" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="110" Height="110">
                                <Image>
                                    <Image.Source>
                                        <BitmapImage UriSource="{Binding Imagen}"/>
                                    </Image.Source>
                                </Image>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" TextWrapping="NoWrap" />
                                </StackPanel>
                                <TextBlock Text="{Binding Direccion}"  TextWrapping="NoWrap"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </Grid>

</Page>

